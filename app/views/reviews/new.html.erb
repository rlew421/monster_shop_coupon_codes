<h1 align= "center">Create Review for <%= link_to @review.item.name, "/items/#{@review.item.id}" %></h1>
<center>
  <%= render partial: 'form', locals: { path: "/items/#{@review.item.id}/reviews" } %>
</center>
